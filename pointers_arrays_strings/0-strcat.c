#include "main.h"

/**
 * dest - an array
 * src - another array
 * _strcat - concatenates two strings
 */
char *_strcat (char *dest, char *src);
{
  char s1[98] = "hello";
  char s2[] = "World";

  char ptr =  _strcat(s1, s2);
 
}
