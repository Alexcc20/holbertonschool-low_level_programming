#include "main.h"

/**
 * dest - an array
 * src - another array
 * _strcat - concatenates two strings
 */
char *_strcat (char *dest, char *src);
{
  char s1[100], s2[100]

    s1 = "hello"
    s2 = "world"

    _strcat(s1, s2);
}
