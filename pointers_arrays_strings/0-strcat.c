#include "main.h"

/**
 * dest - an array
 * src - another array
 * _strcat - concatenates two strings
 */
char *_strcat (char *dest, char *src);
{
  char dest[100] = "hello";
  char src[100] = "world";

  char ptr =  _strcat(dest, src);
 
}
