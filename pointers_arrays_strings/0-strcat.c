#include "main.h"

/**
 * dest - an array
 * src - another array
 * _strcat - concatenates two strings
 */
void _strcat (char *dest, char *src);
{
  char dest[5];
  char src[5];

  _strcat(dest, src);
}
